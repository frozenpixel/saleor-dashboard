server {
    listen       80;
    listen		 9000;
    server_name  dashboard.a6design.net;
    root   /app/dashboard/;

    location /dashboard/ {
        alias /app/dashboard/;
        index  index.html;
        try_files $uri $uri/ /dashboard/index.html;
    }
}
