server {
    listen       80;
    server_name  dashboard.a6design.net;
    root   /app/dashboard/;

    location /dashboard/ {
        alias /app/dashboard/;
        proxy_set_header   X-Forwarded-For $remote_addr;
        proxy_set_header   Host $http_host;
    	proxy_pass http://142.93.59.139:9000;
        index  index.html;
        try_files $uri $uri/ /dashboard/index.html;
    }
}
